[package]
name = "zero2prod_axum"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = { version = "0.7", features = ["http2"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
